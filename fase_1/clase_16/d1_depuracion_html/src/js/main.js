/*
    DEPURACIÓN HTML
*/

const main = document.querySelector('main');

console.log(main); // Muestra el elemento <main> en formato HTML (expandible)
console.dir(main); // Muestra el objeto con sus propiedades (como innerText, id, children, etc.)

