lightbox.option({
    resizeDuration: 300,            // Duración de la animación al cambiar de imagen (en milisegundos)
    wrapAround: true,               // Permite que la galería se reinicie al llegar al final (vuelve al principio)
    albumLabel: 'Imagen %1 de %2'   // Etiqueta del contador de imágenes (usa %1 para el número actual y %2 para el total)
});